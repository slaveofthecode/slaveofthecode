---
import { ui } from "../../i18n/ui";
import { useTranslations } from "../../i18n/utils";

const { lang } = Astro.params;
const t = useTranslations(lang as keyof typeof ui);

const { basics } = t("resume") as any;
---

<section>
	<h1>{t("section.aboutMe")}</h1>
	<p>{basics.summary}</p>
</section>
